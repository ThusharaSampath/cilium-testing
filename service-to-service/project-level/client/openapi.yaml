openapi: 3.0.3
info:
  title: project-level-client
  version: "1.0"
paths:
  /:
    get:
      summary: Calls server /hello and returns the response
      responses:
        "200":
          description: OK
        "502":
          description: Server unreachable
